<div class="container">
	<div class="row">
		<div class="col-xs-12 col-md-6 col-md-offset-3">	
			<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: {class: "form-mail"}) do |f| %>
			  <% devise_flash %>

				<h1>Sign up</h1>
				<div class="form-group">
					<label for="email" class="text-uppercase">Email</label>
					<%= f.email_field :email, autofocus: true, class: "form-control input-lg", placeholder: "Enter email" %>
				</div>

				<div class="form-group">
					<label for="username" class="text-uppercase">Username</label>
					<input type="text" class="form-control input-lg" id="user_username" name="user[username]" placeholder="Enter Username">
				</div>

				<div class="form-group">
					<label for="location" class="text-uppercase">reception center <span title="prefered package reception center" class="glyphicon glyphicon-info-sign"></span></label>
					<%= select_tag(:agency, options_for_select(Agency.all.collect {|p| [ p.location, p.id ] }), {prompt: 'Select a Location' , class: "form-control input-lg", id: "user_agency_id", name: "user[agency_id]" })%>
				</div>
				<div class="form-group">
			 		<%= f.label :password, class: "text-uppercase" %> <% if @validatable %><i>(<%= @minimum_password_length %> characters minimum)</i><% end %><br />
			    	<%= f.password_field :password, autocomplete: "off", class: "form-control input-lg" %>
				</div>

			  	<div class="form-group">
			  		<%= f.label :password_confirmation , class: "text-uppercase"%><br />
			    	<%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control input-lg"  %>
				</div>

			  	<div class="form-group">
			  		<%= f.submit "Sign up", class: "no-radius btn btn-green btn-block btn-lg text-uppercase"%>
			  	</div>
			<% end %>
		</div>
	</div>
</div>
