<div class="container">
	<div class="row">
		<div class="col-xs-12 col-md-4 col-md-offset-4">

		<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "form-mail" }) do |f| %>
			<% devise_flash %>

			<h1><%=t('forgot_password')%></h1>
			<br>
			<div class="form-group">
				<%= f.label :email,t('email'),class: "text-uppercase" %>
				<%= f.email_field :email, autofocus: true, class: "form-control input-lg" %>
			</div>

			<div class="form-group">
				<%= f.submit t('send_instructions') , class: "no-radius btn btn-green btn-block btn-lg text-uppercase"%>
			</div>
		<% end %>

		</div>
	</div>
</div>

